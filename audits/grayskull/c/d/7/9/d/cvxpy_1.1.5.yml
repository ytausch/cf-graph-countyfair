{% set name = "cvxpy" %}
{% set version = 1.1.5 %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/cvxpy-{{ version }}.tar.gz
  sha256: 7c826a874db2e4cefe54e63ebd3a3763d0d72e55a17c7d1cfec80008a87b8d81

build:
  number: 0
  skip: true   # [py2k]
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - numpy >=1.15
    - pip
    - python
  run:
    - ecos >=2
    - numpy >=1.15
    - osqp >=0.4.1
    - python
    - scipy >=1.1.0
    - scs >=1.1.5
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - cvxpy
    - cvxpy.atoms
  commands:
    - pip check
  requires:
    - pip

about:
  home: http://github.com/cvxgrp/cvxpy/
  summary: A domain-specific language for modeling convex optimization problems in Python.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - SteveDiamond
    - akshayka
    - rileyjmurray
    - djsutherland
    - mcg1969
    - scopatz
    - h-vetinari

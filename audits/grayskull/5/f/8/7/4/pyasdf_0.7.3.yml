{% set name = "pyasdf" %}
{% set version = 0.7.3 %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 00df2a207242348977266ecdb1c7fedd6d47511034062f89a6a9d6e2ae376795

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - colorama
    - dill
    - h5py
    - numpy
    - obspy >=1.2.1
    - prov
    - python >=3.7

test:
  imports:
    - pyasdf
    - pyasdf.scripts
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/SeismicData/pyasdf
  summary: Python module for the Adaptable Seismic Data Format (ASDF).
  license: BSD-3-Clause
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - krischer

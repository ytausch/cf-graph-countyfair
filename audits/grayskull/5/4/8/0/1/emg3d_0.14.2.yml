{% set name = "emg3d" %}
{% set version = "0.14.2" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/emg3d-{{ version }}.tar.gz
  sha256: 50ab9c1b7c4c7766709b249dd09d93caf15068c6dc7d4f1ee233fc4251d35d67

build:
  number: 0
  noarch: python
  entry_points:
    - emg3d=emg3d.cli.main:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
    - setuptools_scm
  run:
    - numba >=0.45.0
    - python >=3.7
    - scipy >=1.4.0

test:
  imports:
    - emg3d
    - emg3d.cli
  commands:
    - pip check
    - emg3d --help
  requires:
    - pip

about:
  home: https://empymod.github.io
  summary: A multigrid solver for 3D electromagnetic diffusion.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - prisae

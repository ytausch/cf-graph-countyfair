{% set name = "anyio" %}
{% set version = 2.0.0 %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/anyio-{{ version }}.tar.gz
  sha256: ceca4669ffa3f02bf20ef3d6c2a0c323b16cdc71d1ce0b0bc03c6f1f36054826

build:
  number: 0
  skip: true   # [py2k]
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python
  run:
    - async_generator  # [py<37]
    - dataclasses  # [py<37]
    - idna >=2.8
    - python
    - sniffio >=1.1
    - typing-extensions  # [py<38]

test:
  imports:
    - anyio
    - anyio._backends
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://pypi.org/project/anyio/
  summary: High level compatibility layer for multiple asynchronous event loop implementations
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - CJ-Wright

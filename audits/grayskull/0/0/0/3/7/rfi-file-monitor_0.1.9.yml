{% set name = "rfi-file-monitor" %}
{% set version = 0.1.9 %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/rfi-file-monitor-{{ version }}.tar.gz
  sha256: bdf3d24b30c3ea126a5042dca6c18f72cbe625e74387847bd31814adbcba44ff

build:
  number: 0
  noarch: python
  entry_points:
    - rfi-file-monitor = rfi_file_monitor:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.8
  run:
    - boto3
    - bugsnag
    - munch
    - paramiko
    - pygobject
    - python >=3.8
    - pyyaml
    - watchdog

test:
  imports:
    - rfi_file_monitor
    - rfi_file_monitor.data
  commands:
    - pip check
    - rfi-file-monitor --help
  requires:
    - pip

about:
  home: https://github.com/RosalindFranklinInstitute/rfi-file-monitor
  summary: Easy to use file monitor GUI with user-definable operations
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - tschoonj

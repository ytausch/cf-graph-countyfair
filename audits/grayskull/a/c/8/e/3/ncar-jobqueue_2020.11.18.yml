{% set name = "ncar-jobqueue" %}
{% set version = "2020.11.18" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/ncar-jobqueue-{{ version }}.tar.gz
  sha256: f5960caca4d4d0be1ceb46a4c851eee9ea78b18e97496f6f9e953a7257da8ad9

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
    - setuptools >=30.3.0
    - setuptools_scm
  run:
    - dask-jobqueue >=0.7.1
    - dask>=2.14
    - jinja2
    - python >=3.6
    - pyyaml

test:
  imports:
    - ncar_jobqueue
    - tests
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/NCAR/ncar-jobqueue
  summary: Utilities for expanding dask-jobqueue with appropriate settings for NCAR's clusters
  dev_url: https://github.com/NCAR/ncar-jobqueue
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - andersy005
    - matt-long

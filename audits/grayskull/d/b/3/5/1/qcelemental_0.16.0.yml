{% set name = "qcelemental" %}
{% set version = 0.16.0 %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/qcelemental-{{ version }}.tar.gz
  sha256: 1e3a21ed3d3e0ee9fd5ca893b3855fb53b5c0a34c8cf10d0f84019e6f6ec15f9

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
  run:
    - numpy >=1.12.0
    - pint >=0.10.0
    - pydantic >=1.0.0
    - python >=3.6

test:
  imports:
    - qcelemental
    - qcelemental.data
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/MolSSI/QCElemental
  summary: Essentials for Quantum Chemistry.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - loriab
    - dgasmith
    - lnaden
    - bennybp

{% set name = "pytest-mock" %}
{% set version = 3.3.0 %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pytest-mock-{{ version }}.tar.gz
  sha256: 1d146a6e798b9e6322825e207b4e0544635e679b69253e6e01a221f45945d2f6

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.5
    - setuptools_scm
  run:
    - pytest >=5.0
    - python >=3.5

test:
  imports:
    - pytest_mock
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/pytest-dev/pytest-mock/
  summary: Thin-wrapper around the mock package for easier use with pytest
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - nicoddemus

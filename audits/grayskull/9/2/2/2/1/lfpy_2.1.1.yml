{% set name = "lfpy" %}
{% set version = 2.1.1 %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/LFPy-{{ version }}.tar.gz
  sha256: bdc9fe68090cbf564f8fce2dc102899ad6f9fa31d837c3af6bf5420b8ce6eaba

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - cython
    - numpy
    - pip
    - python
  run:
    - cython >=0.20
    - h5py >=2.5
    - meautility >=1.4.2
    - mpi4py >=1.2
    - neuron >=7.6.4
    - numpy >=1.8
    - python
    - scipy >=0.14
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - LFPy
  commands:
    - pip check
  requires:
    - pip

about:
  home: http://LFPy.readthedocs.io
  summary: A module for modeling extracellular potentials of multicompartment neuron models built on NEURON
  license: GPL-3.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - espenhgn
    - minrk

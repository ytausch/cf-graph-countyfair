{% set name = "pygeoprocessing" %}
{% set version = 2.1.0 %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/pygeoprocessing-{{ version }}.tar.gz
  sha256: ad34effed6c0d6967d522947d9e3058cc642bad713c3875323d1485b5708bee1

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}
  host:
    - cython
    - numpy
    - pip
    - python
    - setuptools_scm
  run:
    - cython
    - gdal >=3.0.4
    - numpy >=1.10.1
    - python
    - rtree >=0.8.3
    - scipy >=0.14.1,!=0.19.1
    - shapely >=1.6.4
    - {{ pin_compatible('numpy') }}

test:
  imports:
    - pygeoprocessing
    - pygeoprocessing.multiprocessing
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/natcap/pygeoprocessing
  summary: 'PyGeoprocessing: Geoprocessing routines for GIS'
  license: BSD-3-Clause
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - phargogh

{% set name = "cf_xarray" %}
{% set version = 0.2.1 %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 9d997f9ee7d7fa3153fecaafad5b41e73e0ff9e55713388cdd2fdaa53cd290fb

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.6
    - setuptools >=41.2
    - setuptools_scm
  run:
    - numpy >=1.15
    - pandas >=0.25
    - python >=3.6
    - setuptools >=41.2

test:
  imports:
    - cf_xarray
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://cf-xarray.readthedocs.io
  summary: A lightweight convenience wrapper for using CF attributes on xarray objects.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - dcherian

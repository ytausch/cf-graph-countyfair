{% set name = "pydsd" %}
{% set version = 1.0.6.2 %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/PyDSD-{{ version }}.tar.gz
  sha256: 6a2a715c19ed707edd98bb67516e58bdcd437b516032efca6cdcfc87c16e8c02

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python
  run:
    - matplotlib-base
    - numpy
    - python
    - pytmatrix >=0.2.0
    - scipy
    - versioneer

test:
  imports:
    - pydsd
    - pydsd.aux_readers
  commands:
    - pip check
  requires:
    - pip

about:
  home: http://pypi.python.org/pypi/PyDSD/
  summary: Python Disdrometer Processing
  license: LGPL-2.1
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - josephhardinee
